#include <stdio.h>#include "./../include/client.h"char *IP = NULL;int portNumber = NULL; int main(int argc, char *argv[]) {    if (argc < 2) {        printf("Wrong use of the program! "                "For help use -h option.\n");        exit(-1);    }    /*For Help*/    if ( (argc == 2) && ( strcmp(argv[1], "-h") == 0 ) ) {        printf("To use the program:\n"                "SuperWavAppClient <IP Direcction> <PortNumber>\n");        exit(0);    }    /*If Client*/    if (( argc >= 3)) {        IP = argv[1];        portNumber = atoi(argv[2]);        printf("\n***********************\n"                        "IP Direction: %s\n"                        "Port Number: %d\n"                        "***********************\n",                IP,portNumber);        startClientConnection(IP,portNumber);    }else {        printf("Wrong use of the program! "                       "For help use -h option.\n"                       "SuperWavApp <IPDirecction> <PortNumber>\n");        exit(-1);    }}